import { FormatBigNumbersPipe } from './format-big-numbers.pipe';

describe('FormatBigNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBigNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
